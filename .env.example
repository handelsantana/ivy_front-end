# Backend articles endpoint (server-side proxy)
IVY_ARTICLES_ENDPOINT=http://localhost:8000/api/v1/articles

# Optional: direct client-side fetch (requires CORS enabled on the backend)
# NEXT_PUBLIC_IVY_ARTICLES_ENDPOINT=https://your-backend/api/v1/articles
